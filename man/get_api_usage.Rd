% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/initialize_python.r
\name{get_api_usage}
\alias{get_api_usage}
\title{Report daily Research API usage}
\usage{
get_api_usage()
}
\value{
A named integer vector with three elements:
\describe{
\item{\code{calls}}{Number of Research API calls made today.}
\item{\code{remaining}}{Calls remaining before the daily limit is reached.}
\item{\code{limit}}{The daily limit (always \code{10000}).}
}
}
\description{
Returns the number of Google Trends Research API calls made today, the
number remaining before the daily limit is reached, and the limit itself.
The counter is stored in \code{gt.env} and resets automatically when the
calendar date changes.
}
\details{
The counter is incremented once per successful call to the internal helpers
\code{.get_trend()}, \code{.get_region()}, and \code{.get_related()} whenever the Research
API backend is active (i.e., after \code{\link[=initialize_python]{initialize_python()}} has been called).
Calls routed through the default \code{gtrendsR} scraping backend are not counted.

The daily limit of 10,000 calls is set by Google. The counter does \strong{not}
enforce this limit; it only tracks usage so that callers can monitor their
consumption.
}
\examples{
get_api_usage()

}
\seealso{
\code{\link[=initialize_python]{initialize_python()}} to enable the Research API backend.
}
