% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_related.r
\name{download_related}
\alias{download_related}
\alias{download_topics}
\alias{download_themes}
\alias{download_topics_rising}
\alias{download_themes_rising}
\alias{download_topics_global}
\alias{download_themes_global}
\alias{download_topics_rising_global}
\alias{download_themes_rising_global}
\title{Download related topics and themes (including rising variants)}
\usage{
download_topics(object, locations = NULL)

download_themes(object, locations = NULL)

download_topics_rising(object, locations = NULL)

download_themes_rising(object, locations = NULL)

download_topics_global(object)

download_themes_global(object)

download_topics_rising_global(object)

download_themes_rising_global(object)
}
\arguments{
\item{object}{Numeric scalar/vector or list of numeric scalars. Object batch
id(s) (\code{batch_o}) to download.}

\item{locations}{Character vector of location codes. Defaults to
\code{gt.env$countries} when available; otherwise \code{globaltrends::countries}.
Use \code{"world"} to request global data (\code{"world"}).}
}
\value{
Invisibly returns \code{TRUE}. Called for its side effects (writing to
\code{data_related}) and emits a message per location.
}
\description{
Convenience wrappers around \code{\link[=download_related]{download_related()}} that download one of four
related-queries variants for the keywords in one or more object batches:
\itemize{
\item related topics (top)
\item related themes (top)
\item related topics (rising)
\item related themes (rising)
}

All downloads are written to the database table \code{data_related}.
}
\details{
This feature requires the Research API backend (Python) initialized via
\code{\link[=initialize_python]{initialize_python()}}. The internal \code{.get_related()} helper is used to query
related topics/themes for each keyword in the object batch and for each
requested location.

The function avoids duplicate downloads by skipping locations already present
in \code{data_related} for the requested \code{batch_o} (optionally, you may want to
extend de-duplication to also include the \code{topic}/\code{rising} dimensions; see
notes in code).
}
