% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_related.r
\name{download_related}
\alias{download_related}
\alias{download_related.numeric}
\alias{download_related.list}
\alias{download_topics}
\alias{download_themes}
\alias{download_topics_rising}
\alias{download_themes_rising}
\alias{download_topics_global}
\alias{download_themes_global}
\alias{download_topics_rising_global}
\alias{download_themes_rising_global}
\title{Download related topics and themes for object keyword batches}
\usage{
download_related(object, locations = NULL, topic = NULL, rising = NULL)

download_topics(object, locations = NULL)

download_themes(object, locations = NULL)

download_topics_rising(object, locations = NULL)

download_themes_rising(object, locations = NULL)

download_topics_global(object)

download_themes_global(object)

download_topics_rising_global(object)

download_themes_rising_global(object)

\method{download_related}{numeric}(object, locations = NULL, topic = NULL, rising = NULL)

\method{download_related}{list}(object, locations = NULL, topic = NULL, rising = NULL)
}
\arguments{
\item{object}{Numeric scalar, numeric vector, or list of numeric scalars.
The object batch id(s) to download. A scalar downloads a single batch; a
vector or list downloads multiple batches sequentially. Must refer to
batches already registered via \code{\link[=add_keyword]{add_keyword()}}.}

\item{locations}{Character vector of ISO 3166-1 alpha-2 location codes.
Defaults to \code{gt.env$countries} when set by \code{\link[=start_db]{start_db()}}; otherwise falls
back to \code{globaltrends::countries}. Pass \code{"world"} (or use the \verb{_global}
convenience wrappers) to download the worldwide aggregate instead of
country-level data.}

\item{topic}{Logical scalar. \code{TRUE} to download related \emph{topics}; \code{FALSE} to
download related \emph{themes}. The convenience wrappers set this automatically.}

\item{rising}{Logical scalar. \code{TRUE} to download \emph{rising} (breakout) results;
\code{FALSE} to download \emph{top} results. The convenience wrappers set this
automatically.}
}
\value{
Invisibly returns \code{TRUE}. The function is called for its side effects:
downloaded rows are appended to \code{data_related} in the active database, and
one progress message is emitted per location indicating whether data was
written or no data was returned.
}
\description{
Downloads Google Trends related topics or themes for one or more \emph{object}
batches across a set of locations and appends the results to the database
table \code{data_related}. Convenience wrappers cover all four combinations of
\code{topic} (topics vs. themes) and \code{rising} (top vs. rising):
\itemize{
\item \code{\link[=download_topics]{download_topics()}} / \code{\link[=download_topics_global]{download_topics_global()}} — top related topics
\item \code{\link[=download_themes]{download_themes()}} / \code{\link[=download_themes_global]{download_themes_global()}} — top related themes
\item \code{\link[=download_topics_rising]{download_topics_rising()}} / \code{\link[=download_topics_rising_global]{download_topics_rising_global()}} — rising
related topics
\item \code{\link[=download_themes_rising]{download_themes_rising()}} / \code{\link[=download_themes_rising_global]{download_themes_rising_global()}} — rising
related themes
}
}
\details{
\strong{Prerequisites.} \code{\link[=initialize_python]{initialize_python()}} must be called before
\code{download_related()} to set up the Research API backend. \code{\link[=start_db]{start_db()}} must
also have been called; it connects to the database and populates
\code{gt.env$keywords_object} and \code{gt.env$time_object} from the tables
\code{batch_keywords} and \code{batch_time} (created via \code{\link[=add_keyword]{add_keyword()}}). These
in-memory objects are used to look up the keywords and time window for each
requested batch.

\strong{Dispatch.} \code{download_related()} is an S3 generic that dispatches on the
class of \code{object}. Passing a numeric scalar routes to the \code{.numeric} method,
which performs the actual download. Passing a numeric vector of length > 1
coerces \code{object} to a list and delegates to the \code{.list} method, which
iterates over batches sequentially. Passing a list directly also routes to
the \code{.list} method.

\strong{Download backend.} Requests are issued through the internal \code{.get_related()}
helper, which queries the Google Trends Research API via the Python backend.
One API call is made per keyword per location; results across keywords are
row-bound before being written to \code{data_related}.

\strong{Deduplication.} Before downloading, the function queries \code{data_related}
for locations already present for the requested batch (filtered by \code{topic}
and \code{rising}). Only locations not yet in the database are downloaded. If all
locations are already present, the function returns early with a message and
no requests are made.

\strong{Missing data.} If the API returns no data for a location (e.g. due to
insufficient search volume), the result for that location is skipped
(nothing is written to \code{data_related}) and a "No data returned" message is
emitted.
}
\examples{
\dontrun{
# Download top related topics for one object batch across all countries
download_topics(object = 1, locations = countries)

# Download rising related themes for several batches sequentially
download_themes_rising(object = as.list(1:5), locations = countries)

# Download top related topics worldwide
download_topics_global(object = 1)
}

}
\seealso{
\code{\link[=initialize_python]{initialize_python()}} to set up the Python backend (required before use).
\code{\link[=start_db]{start_db()}} to connect to the database and populate \code{gt.env}.
\code{\link[=add_keyword]{add_keyword()}} to register object batches before downloading.
\code{\link[=download_object]{download_object()}} to download raw search volume data for object keywords.
}
