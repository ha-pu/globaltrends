% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database_functions.r
\name{initialize_db}
\alias{initialize_db}
\title{Initialize the local database store}
\usage{
initialize_db()
}
\value{
Invisibly returns \code{TRUE}. Called for its side effects (creating
files under \verb{db/}).
}
\description{
Creates the local database store used by \code{globaltrends} in the current
working directory and initializes all required tables.
}
\details{
The package uses \code{data.table} objects persisted as a single RDS file under
the \verb{db/} folder. \code{initialize_db()} creates 10 empty tables, populates
default location sets, and saves the result to \code{db/globaltrends.rds}.

If the RDS file already exists the function returns early without
overwriting anything.

Default location sets written to \code{data_locations}:
\describe{
\item{\code{countries}}{ISO 3166-1 alpha-2 codes for countries above the GDP
share threshold (see \link{countries}).}
\item{\code{us_states}}{ISO 3166-2 codes for US states and Washington DC
(see \link{us_states}).}
}
}
\examples{
\dontrun{
initialize_db()
start_db()
}

}
\seealso{
\code{\link[=start_db]{start_db()}} to open a working session after initialization;
\code{\link[=disconnect_db]{disconnect_db()}} to persist changes and close the session.
}
