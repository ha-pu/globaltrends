% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.r
\docType{data}
\name{data_control}
\alias{data_control}
\alias{example_control}
\title{Example table: control downloads (\code{data_control})}
\format{
A tibble with 5 variables:
\describe{
\item{location}{Character. Location code (ISO 3166-1 alpha-2 or other
codes supported by Google Trends). Global data uses \code{"world"}.}
\item{keyword}{Character. Control keyword.}
\item{date}{Integer. Date stored as days since 1970-01-01 (Unix epoch).
Convert with \code{as.Date(date, origin = "1970-01-01")}.}
\item{hits}{Integer. Relative search interest in [0, 100]. Google Trends
normalizes all values within a single query window so the peak observation
equals 100.}
\item{batch}{Integer. Control batch id.}
}
}
\source{
Google Trends (\url{https://trends.google.com}). Simulated to match
empirical distributional statistics from real downloads.
}
\usage{
example_control
}
\description{
Example data representing the database table \code{data_control}.
Each row contains Google Trends \code{hits} for a control \code{keyword} in a given
\code{location} on a given \code{date}, along with the control \code{batch} identifier.

In a live database, data are downloaded via \code{\link[=download_control]{download_control()}} and are
accessible via \verb{gt.env$dt_*} tables after \code{\link[=start_db]{start_db()}}. Global
aggregates use \code{"world"} as \code{location}.

The example dataset is simulated to resemble real Google Trends output.
Simulated values are bounded to the empirical [min, max] range observed in
actual downloads for each keyword--location pair.
}
\seealso{
\code{\link[=download_control]{download_control()}}, \code{\link[=start_db]{start_db()}}
}
\keyword{datasets}
